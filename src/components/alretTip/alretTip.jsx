import React,{Component} from 'react'import {connect} from 'react-redux'import {cancelAlertShow} from '../../redux/actions'import './alretTip.styl' class AlertTip extends Component{   cancelAlert =() => {    this.props.cancelAlertShow(false)  }  render() {    const {alertText} = this.props    return (      <div className="alert_container">        <section className="tip_text_container">          <div className="tip_icon">            <span></span>            <span></span>          </div>          <p className="tip_text">{alertText}</p>          <div className="confrim" onClick={this.cancelAlert}>确认</div>  </section>  </div>    )  }}export default connect(  state => ({alertText:state.alerttiptext.alertText}),  {cancelAlertShow})(AlertTip)