import React,{Component} from 'react'import PropTypes from 'prop-types'import {withRouter,Link} from 'react-router-dom'import './footerGuide.styl'class FooterGuide extends Component{    static propTypes = {        guideList: PropTypes.array.isRequired    }    render() {        const {guideList} = this.props // 获取通过props传递过来的数据        const {pathname} = this.props.location // 获取当前的        // 动态数据需要用{}        return (            <div id='guideWrap'>                <ul className='guideList'>                    {                        guideList.map((item,index)=> {                            const classname = `iconfont ${item.icon}`                        return (                          <Link to={item.path} key={index}>                            <li key={index} className={pathname===item.path?'on':null}>                                <i className={classname} ></i>                                  {/*<span className={pathname===item.path? 'active':null}></span>*/}                                  <p>{item.title}</p>                                  </li>                                  </Link>                        )                    })                    }                </ul>            </div>        )    }}export default withRouter(FooterGuide)