import React,{Component} from 'react'import {withRouter} from 'react-router-dom'import PropTypes from 'prop-types'import './goodClassList.styl'class GoodClassList extends Component{  state = {    color: ['red','#f48f18','#e5686d']  }  static propTypes = {    cateList: PropTypes.array.isRequired  }  render(){    const {cateList} = this.props    const {color} = this.state    return (      <div className="classListWrap">        {          cateList.map((cate,index) => {            return (              <div className="ListWrap" key={index}>                <section className="listTop" >                  <span>                    {cate.name}好物                  </span>                </section>                <section className="goodMsg">                  <ul>                    {                      cate.itemList.map( (item,index) => {                        if (index<7){                          return (                            <li key={index}>                              <div className="goodPic">                                <img src={item.primaryPicUrl} />                              </div>                              <div className="slogan">                                <span>{item.simpleDesc}</span>                              </div>                              <div className="titleMsg">                                {                                  item.itemTagList.map((tag,index) => {                                    return (                                      <span className='disCounts' key={index}>                                    <p style={{background: `${color[tag.type]}`}}>{tag.name}</p>                                  </span>                                    )                                  })                                }                                <span className="goodName">                            {item.name}                          </span>                                <span className="goodPrice">                            ￥{item.retailPrice}                          </span>                              </div>                            </li>                          )                        }                    })}                    <li className="more">                      <div className="text">                        <span>                          更多居家好物                        </span>                        <i className='icon'></i>                      </div>                    </li>                  </ul>                </section>              </div>            )          })        }  </div>    )  }}export default withRouter(GoodClassList)